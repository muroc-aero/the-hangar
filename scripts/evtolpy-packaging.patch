diff --git a/pyproject.toml b/pyproject.toml
new file mode 100644
index 0000000..db31d9f
--- /dev/null
+++ b/pyproject.toml
@@ -0,0 +1,13 @@
+[project]
+name = "evtolpy"
+version = "0.1.0"
+description = "Electric VTOL aircraft sizing and mission-energy analysis"
+requires-python = ">=3.9"
+dependencies = ["matplotlib"]
+
+[build-system]
+requires = ["hatchling"]
+build-backend = "hatchling.build"
+
+[tool.hatch.build.targets.wheel]
+packages = ["evtol"]
